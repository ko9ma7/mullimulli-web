# v4.2 — 기기 간 친구 동기화 보호 + 오는 편지 알림

## 핵심 수정

- 공개 GitHub Pages의 `docs/config.js`에 실제 Worker API 주소를 기본값으로 넣었습니다.
- 공개 사이트에서 API가 끊겼을 때 LocalStorage 계정으로 조용히 대체하지 않습니다. 연결 오류 화면을 보여 로컬 계정을 실제 계정으로 오해하는 상황을 막습니다.
- Worker의 D1 binding과 GitHub Pages origin을 현재 프로젝트 값으로 고정했습니다.
- 친구 검색/추가 데이터는 Cloudflare D1을 기준으로 동작하므로 다른 컴퓨터, 다른 인터넷 회선에서도 동일한 계정을 조회합니다.
- 나에게 오는 중인 편지를 모든 탭에서 확인할 수 있는 배너를 추가했습니다.
- 받은편지 메뉴에 여행 중 편지 개수 배지를 표시합니다.
- 오는 편지는 본문을 공개하지 않고 보낸 사람, 전달자, D-Day, 남은 시간, 도착 예정일만 보여줍니다.
- 페이지가 열려 있는 동안 10초마다 서버 상태를 갱신해 새 편지를 감지합니다.
- 사용자가 허용하면 브라우저 알림도 표시합니다. 브라우저가 완전히 종료된 상태의 Web Push는 이번 버전 범위에 포함하지 않습니다.

## 배포

기존 D1 데이터와 MESSAGE_KEY를 유지한 채 `UPDATE_V4_2.cmd`를 실행하면 Worker와 Pages를 순서대로 갱신합니다.
