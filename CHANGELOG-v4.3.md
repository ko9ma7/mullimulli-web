# 멀리멀리 v4.3

## 온라인 계정 서버 복구

- v4.2의 `/api/health`가 Worker 연결만 확인하고 D1은 확인하지 않던 문제를 수정했습니다.
- 이제 health가 `users / sessions / friends / messages` 테이블과 필수 `users` 컬럼까지 실제로 검사합니다.
- D1이 불완전하면 사이트가 가입 화면을 띄우지 않고 `온라인 DB 점검이 필요합니다`라고 명확히 표시합니다.
- `REPAIR_ONLINE.cmd`를 추가했습니다. 기존 계정·친구·편지를 삭제하지 않고 누락 테이블/안전한 컬럼을 보완한 뒤 Worker를 재배포합니다.
- 복구 스크립트는 마지막에 실제 `/api/health`가 `ok:true`인지까지 검증합니다.
- 다른 PC에서도 친구 검색이 되려면 양쪽 계정이 모두 온라인 D1 계정이어야 합니다. 로컬 계정은 복구 후 `온라인으로 옮기기` 또는 온라인 재가입이 필요합니다.

## 받은 편지 기대감 기능

- v4.2의 `나에게 오는 편지` / 받은편지 배지 / 브라우저 알림 기능은 그대로 유지됩니다.
- 수신자는 도착 전 본문은 볼 수 없고, 보내는 사람·전달자·D-Day·남은 시간·도착 예정만 확인합니다.
