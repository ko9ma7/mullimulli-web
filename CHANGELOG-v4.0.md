# v4.0 변경사항

## 배포 문제 수정

- GitHub Pages용 `.github/workflows/deploy.yml` 포함 및 필수 파일 검증에 `version.json`, `version.txt`를 추가했습니다.
- `docs/index.html`의 CSS/JS 캐시 버전을 `v=4.0.0`으로 올렸습니다.
- 배포 성공 여부를 URL에서 바로 확인할 수 있도록 `docs/version.txt`와 `docs/version.json`을 추가했습니다.
- `SETUP_ONLINE.cmd`는 기존 Worker/D1을 재사용하면서 최신 프로젝트를 GitHub에 올리고 Pages Workflow를 다시 실행합니다.

## UI 재구성

- 제공된 4번째 디자인 시안의 레이아웃을 실제 코드 구조에 반영했습니다.
- 데스크톱에서 오른쪽 프로필/친구 패널이 Hero와 같은 높이에서 시작합니다.
- Hero 높이를 줄이고, 보내기 화면을 첫 화면 안에 더 많이 노출합니다.
- 받는 사람 + 메시지 작성과 전달자 6개 추천을 같은 작업 영역에서 동시에 확인할 수 있습니다.
- 전체 50개 전달자는 사용자가 `전체 50개 보기`를 눌렀을 때만 확장됩니다.
- 선택 전달자 요약에 성공률, 거리, 예상 서비스 시간, D-Day, 실제 남은 시간, 정확한 도착 예정 년/월/일/요일/시간을 함께 표시합니다.
- 발송 버튼을 요약 영역 오른쪽에 배치했습니다.
- 진행 중인 여정은 3열 카드로 요약하고, 각 카드에 진행률, 출발/도착 일시, D-Day, 남은 시간을 표시합니다.
- 여정 전체 화면은 출발 일시 → 총 소요시간 → 도착 예정 일시와 D-Day를 큰 타임라인 카드로 표시합니다.
- 모바일은 한 열로 재배치하고 고정 하단 내비게이션을 유지합니다.

## 버전 확인

배포 후 아래 주소가 `MULLIMULLI 4.0.0`으로 시작해야 최신 버전입니다.

```text
https://ko9ma7.github.io/mullimulli-web/version.txt
```
